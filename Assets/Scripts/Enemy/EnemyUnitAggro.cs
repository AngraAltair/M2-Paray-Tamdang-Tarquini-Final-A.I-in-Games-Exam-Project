using UnityEngine;

// This script is for detecting aggression for combat behaviors between Enemy Units and Player Units.
public class EnemyUnitAggro : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // void OnTriggerEnter(Collider other)
    // {
    //     if (other.gameObject.GetComponent<AIBehavior>() != null && other.gameObject.GetComponent<AIAgression>() != null)
    //     {
    //         Debug.Log("Player unit sighted");
    //     }
    // }
}
